---
# The Linux task builds llvm from source; brew's bottle is close enough here,
# and the Command Line Tools already provide an Apple clang. Opt in with
# `make install TAGS=llvm`.
- name: install llvm
  become: false
  community.general.homebrew:
    name: llvm
    state: present
