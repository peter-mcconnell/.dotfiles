---
- name: update apt sources
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 86400

- name: install deps
  ansible.builtin.apt:
    name: "{{ base_packages }}"
    state: present
